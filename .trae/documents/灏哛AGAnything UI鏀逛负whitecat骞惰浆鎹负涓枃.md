## 计划目标
1. 将项目名称从 "RAGAnything" 统一改为 "whitecat"
2. 将UI中的所有英文文本转换为中文
3. 保持原有的功能和设计不变

## 实施步骤

### 1. 修改基础模板（base.html）
- 将页面标题从 "RAGAnything UI" 改为 "whitecat 智能系统"
- 将导航栏中的项目名称从 "RAGAnything" 改为 "whitecat"
- 将导航菜单中的英文文本改为中文
- 将HTML语言从 "en" 改为 "zh-CN"

### 2. 修改首页（index.html）
- 将欢迎标题和描述改为中文
- 将快速操作按钮文本改为中文
- 将核心功能和最近活动的文本改为中文

### 3. 修改查询页面（query.html）
- 将页面标题和描述改为中文
- 将表单标签、按钮文本改为中文
- 将查询示例改为中文
- 将查询结果相关文本改为中文

### 4. 修改知识图谱页面（knowledge_graph.html）
- 将页面标题和描述改为中文
- 将控制按钮和过滤选项改为中文
- 将可视化相关文本改为中文

### 5. 修改配置页面（config.html）
- 将页面标题和描述改为中文
- 将表单标签和选项改为中文
- 将配置说明改为中文

### 6. 修改实体页面（entities.html）
- 将页面标题和描述改为中文
- 将操作按钮和过滤选项改为中文
- 将表格列标题和提示信息改为中文
- 将模态框中的表单文本改为中文

### 7. 修改关系页面（relationships.html）
- 将页面标题和描述改为中文
- 将操作按钮和过滤选项改为中文
- 将表格列标题和提示信息改为中文
- 将模态框中的表单文本改为中文

### 8. 修改版本控制页面（version_control.html）
- 将页面标题和描述改为中文
- 将操作按钮和搜索选项改为中文
- 将表格列标题和提示信息改为中文
- 将模态框中的表单文本改为中文

### 9. 修改推理规则页面（reasoning_rules.html）
- 将页面标题和描述改为中文
- 将操作按钮和过滤选项改为中文
- 将表格列标题和提示信息改为中文
- 将模态框中的表单文本改为中文

## 技术要点
- 保持原有的HTML结构和CSS样式不变
- 保持原有的JavaScript功能不变
- 确保中文文本在各种屏幕尺寸下都能正常显示
- 确保所有链接和按钮功能正常

## 验证方法
- 检查所有页面的标题和内容是否正确转换为中文
- 检查所有导航链接是否正常工作
- 检查所有表单和按钮功能是否正常
- 检查所有提示信息是否正确显示为中文